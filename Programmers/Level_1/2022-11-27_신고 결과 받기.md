# 신고 결과 받기 - 실패(3개 시간 초과)
## 문제 설명
https://school.programmers.co.kr/learn/courses/30/lessons/92334

## 제출
```swift
import Foundation

func solution(_ id_list:[String], _ report:[String], _ k:Int) -> [Int] {
    var dict: [String: [String]] = [:]
    var arr: [Int] = Array(repeating: 0, count: id_list.count)
    var ids: [String: Int] = [:]
    
    // 이름에 해당하는 index dictionary 생성
    for (index, id) in id_list.enumerated() {
        ids[id] = index
    }
    
    // [ 신고당한사람 : [신고자] ]
    for repo in report {
        let arr = repo.split(separator: " ").compactMap { String($0) }
        let name = arr[1]
        let singo = arr[0]
        if dict[name] == nil {
            dict.updateValue([singo], forKey: name)
        } else {
            if !dict[name]!.contains(singo) {
                dict[name]!.append(singo)
            }
        }
    }


    for name in dict.keys {
        if dict[name]!.count >= k {
            for n in dict[name]! {
                arr[ids[n]!] += 1
            }
        }
    }
    
    return arr
}
// 시간초과 
/*
 import Foundation

 func solution(_ id_list:[String], _ report:[String], _ k:Int) -> [Int] {
     var 신고받은횟수 = [String: Int]()
     var 결과 = [String: Int]()
     var 신고결과 = [String: [String]]()
     
     id_list.forEach {
         신고받은횟수[$0] = 0
         결과[$0] = 0
         신고결과[$0] = [String]()
     }
     
     Set(report).sorted().forEach {
         let array = $0.split(separator: " ").map { String($0) }
         
         guard var value = 신고결과[array.first!] else { return }
         value.append(array.last!)
         신고결과[array.first!] = value
         
         guard var num = 신고받은횟수[array.last!] else { return }
         num += 1
         신고받은횟수[array.last!] = num
     }
     
     신고받은횟수.filter { $0.value >= k }.map { $0.key }.forEach { element in
         신고결과.forEach {
             if $0.value.contains(element) {
                 guard var num = 결과[$0.key] else { return }
                 num += 1
                 결과[$0.key] = num
             }
         }
     }
     
     return 결과.sorted { (first, second) in
         return id_list.firstIndex(of: first.key)! < id_list.firstIndex(of: second.key)!
     }.map { $0.value }
}
 */

```
